/**
 * Re-exports role styles and icons from the centralized role config.
 * Kept as a thin shim so existing local imports within admin/users continue to work.
 */
export { ROLE_STYLES, ROLE_ICONS } from "@/lib/role-config";
